# Map subleq_cpu to a richer generic gate set (AND/OR/XOR/MUX/...) for a second opinion.
read_verilog rtl/subleq_cpu.v
hierarchy -top subleq_cpu
proc; opt; fsm; opt; memory; opt
techmap; opt -full
dfflegalize -cell $_DFF_P_ 0
abc -g AND,NAND,OR,NOR,XOR,XNOR,ANDNOT,ORNOT,MUX
opt_clean
stat
